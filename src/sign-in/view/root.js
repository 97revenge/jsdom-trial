export default function root() {
  const root = document.querySelector("web-form");
  root.setAttribute(
    "style",
    `margin-top:50px; 
     margin-left:0px;
     margin-left:0px;
     justify-content:center;
    `
  );

  return root;
}
